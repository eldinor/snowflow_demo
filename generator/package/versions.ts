/** Increment when generator algorithms change output for an existing seed. */
export const GENERATOR_VERSION = 1;

/** Increment when the serialized world-package layout changes. */
export const PACKAGE_VERSION = 1;
